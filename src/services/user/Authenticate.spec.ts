import { describe, it } from '@jest/globals'

describe('authenticate user service [unit]', () => {
  it.todo('should throw bad request exception when user isnt found')
  it.todo('should throw bad request exception when passwords isnt equal')
  it.todo('should be able to authenticate user')
})
